pipeline {
  agent any
  stages {
    stage('TRy') {
      steps {
        sh 'git config -l'
      }
    }

    stage('gitcon') {
      steps {
        sh 'ghi open -m "this is a new issue\\n a new issue"'
      }
    }

  }
}